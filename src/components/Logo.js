import React from 'react';
import { Link } from "react-router-dom";

const Logo = () => (
    <Link to="/" className="navbar-brand">
        <span>Manfred Marcano</span>
    </Link>
);

export default Logo;